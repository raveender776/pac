pipeline {
 agent any
  stages{
    stage("working with first stage")
    {
      steps{
        script{
         var = 10
         println "here is your var value ${var}"
        }
      }
    }
  }
}
